import { mdx } from '@mdx-js/react';
import { theme } from '~/all';
import { radii } from '~/radii';
import { SystemUIShowcases } from '@divriots/dockit-react/systemui-showcases';
import { MdxLayout } from '~/layout';
export default MdxLayout;

export const RadiiDefinition = () => (
  <pre>
    <code>{JSON.stringify(radii, null, 2)}</code>
  </pre>
);

# Radii

Values defined as an array.

## Definition

<RadiiDefinition />

## Showcases

<SystemUIShowcases
  theme={theme}
  showcaseKey="borderRadius"
  componentProps={{
    width: '8rem',
    height: '8rem',
    boxShadow: '0 12px 48px rgba(149, 157, 165, 0.3)',
    bg: '#22863a',
  }}
/>
