import { mdx } from '@mdx-js/react';
import { theme } from '~/all';
import { space } from '~/space';
import { SystemUIShowcases } from '@divriots/dockit-react/systemui-showcases';
import { MdxLayout } from '~/layout';
export default MdxLayout;

export const SpaceDefinition = () => (
  <pre>
    <code>{JSON.stringify(space, null, 2)}</code>
  </pre>
);

# Space

Defined as array of values.

## Definition

<SpaceDefinition />

## Showcases

<SystemUIShowcases theme={theme} showcaseKey="space" />
