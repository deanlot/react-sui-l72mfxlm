import { mdx } from '@mdx-js/react';
import { theme } from '~/all';
import { zIndex } from '~/z-index';
import { SystemUIShowcases } from '@divriots/dockit-react/systemui-showcases';
import { MdxLayout } from '~/layout';
export default MdxLayout;

export const ZIndexDefinition = () => (
  <pre>
    <code>{JSON.stringify(zIndex, null, 2)}</code>
  </pre>
);

# Z-Index

Named values defined as a plain object.

## Definition

<ZIndexDefinition />

## Showcases

# z-index

<SystemUIShowcases theme={theme} showcaseKey="zIndex" />
